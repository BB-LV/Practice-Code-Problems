/*
Make UpperCase

Write a function which converts the input string to uppercase.

This kata is giving us a single string as an input and asking us to return a single
string back. The return string must be all capitalized.

We can make use of the .toUpperCase() method to return the input string.

*/

function makeUpperCase(str) {
    return str.toUpperCase();
  }